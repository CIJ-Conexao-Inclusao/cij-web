import { Box } from "@mui/material";
import styled from "@emotion/styled";

export const BoxDeficienciesPerCity = styled(Box)({
    marginBottom: "1rem",
    width: "100%"
});

export const BoxDeficiencyPerCity = styled(Box)({
    alignItems: "center",
    backgroundColor: "#EEEEEE",
	borderRadius: "10px",
	boxShadow: "5px 5px 10px 0 #00000025",
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    padding: "1rem"
});
